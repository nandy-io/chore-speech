{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "daemon",
            "type": "python",
            "request": "attach",
            "port": 26751,
            "host": "localhost",
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}/daemon",
                    "remoteRoot": "/opt/service"
                }
            ]
        }
    ]
}